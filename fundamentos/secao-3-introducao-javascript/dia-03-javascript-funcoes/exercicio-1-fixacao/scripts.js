let saldo = 0;

function adicionarSaldo(valor) {
    saldo += valor;
}

function subtrairSaldo(valor) {
    saldo -= valor;
}

function multiplicarSaldo(taxa) {
    saldo *= taxa;
}

function dividirSalvo(valor) {
    saldo /= valor;
}